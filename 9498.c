#include <stdio.h>
int main()
{
	int score = 0; {
		scanf_s("%d", &score);
		if (89 < score)
			printf("A");
		else if (79 < score)
		printf("B");
		else if (69 < score)
		printf("C");
		else if (59 < score)
			printf("D");

		else printf("F");
	}


	return 0;

}